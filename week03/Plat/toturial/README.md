# Router Basic Explain
 제가 예시로 주신 Data Router방식을 안 보고 제작해버려서, 기존에 사용했던 Declarative Router방식을 사용해서 아래와 같은 방식으로 구현했습니다.

# 예시 코드
```
<BrowserRouter>
  <Routes>
    <Route path="/" element={<Home />} />
  </Routes>
</BrowserRouter>
```
